name: Keep Supabase Active

on:
  schedule:
    # يعمل تلقائيًا كل 3 أيام في الساعة 12 ظهرًا UTC
    - cron: '0 12 */3 * *'
  workflow_dispatch: # يسمح لك بتشغيله يدويًا للاختبار في أي وقت

jobs:
  ping-supabase:
    runs-on: ubuntu-latest
    steps:
      - name: Send Keep-Alive Request to Supabase
        run: |
          curl -i -X GET "https://vmxbedovoahuggfpcmyk.supabase.co/rest/v1/" \
            -H "apikey: ${{ secrets.SUPABASE_ANON_KEY }}" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}"
